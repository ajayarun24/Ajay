#include<bits/stdc++.h>
#pragma once

using namespace std;


class minPath{
    public:
    add_edge(vector<vector<Parsing::Location*>> adj, int source, int destination);
    void printShortestDistance(vector<vector<Parsing::Location*>> adj, int s, int dest, int v);
    
}
